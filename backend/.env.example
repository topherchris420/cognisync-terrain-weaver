# Comma-separated list of origins allowed to call this API from the browser.
ALLOWED_ORIGINS=http://localhost:5173,https://your-app.lovable.app

# Optional: LLM provider for AI-generated adaptation recommendations.
# Leave blank to use the built-in rule-based fallback.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
